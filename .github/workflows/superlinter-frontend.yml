---

# Managed by gridwatch/infrastructure-github.
# Manual edits will be overwritten on the next `terraform apply`.

name: Frontend Super-Linter

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/CODEOWNERS'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
  pull_request:

permissions:
  contents: read

jobs:
  superlinter:
    name: superlinter
    uses: gridwatch/.github/.github/workflows/superlinter-frontend.yml@a28ffc1406d945d76db723930ffff901e42f2dc8  # v20260917-3
