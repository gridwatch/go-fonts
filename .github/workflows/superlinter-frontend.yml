---

# Managed by gridwatch/infrastructure-github.
# Manual edits will be overwritten on the next `terraform apply`.

name: Frontend Super-Linter

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/CODEOWNERS'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
  pull_request:

permissions:
  contents: read

jobs:
  superlinter:
    name: superlinter
    uses: gridwatch/.github/.github/workflows/superlinter-frontend.yml@afdbceffd15291ccee6f951bb40d27d76f61efda  # v20260917-1
