---

# Managed by gridwatch/infrastructure-github.
# Manual edits will be overwritten on the next `terraform apply`.

name: Frontend Super-Linter

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/CODEOWNERS'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
  pull_request:

permissions:
  contents: read

jobs:
  superlinter:
    name: superlinter
    uses: gridwatch/.github/.github/workflows/superlinter-frontend.yml@5365ba777c7ec00d70e385989818525c81d5f9ba  # v20260923-1
