---

# Managed by gridwatch/infrastructure-github.
# Manual edits will be overwritten on the next `terraform apply`.

name: Frontend Super-Linter

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/CODEOWNERS'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
  pull_request:

permissions:
  contents: read

jobs:
  superlinter:
    name: superlinter
    uses: gridwatch/.github/.github/workflows/superlinter-frontend.yml@00839f1e507cd21bf6694b995e22ec8d81856188  # v20260925-3
